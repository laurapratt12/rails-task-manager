<h1><%= "My tasks" %></h1>
<ul class= "list-group">
  <% @tasks.each do |task| %>
    <li class="list-group-item d-flex justify-content-between align-items-center"><%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>
<%= link_to "Create a new task", new_task_path %>
